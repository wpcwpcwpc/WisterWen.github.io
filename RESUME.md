# RESUME —— 简历上站与脱敏边界

**结论先行：简历本身不脱敏，原样公开。** 简历是主动投递给雇主的对外材料，
姓名、联系方式、雇主名称本来就公开；把公司名泛化成"某公司"反而让人看不出你做过什么。

脱敏只针对**公司业务资产**：内部产品/系统名、内部项目代号、内网地址路径、业务指标精确值。
边界定义见 `GLOSSARY.md` 第〇节。

## 一、上站流程

```bash
# 1. 简历 PDF 放到这里（路径固定，站点按存在性出入口）
#    public/resume/resume.pdf

# 2. 审计：不拦构建，但会把"这份 PDF 里哪些内容会公开"逐条列出来
npm run pdf-audit

# 3. 构建：关于页自动出现"下载简历（PDF）"入口
npm run build
```

- 入口位置：`/about#resume`（顶部导航「简历」直达）
- 产物缺失时显示"完整简历可邮件索取"，不会出现死链
- `public/resume/resume.pdf` 在审计里是**已豁免文档**：命中只告警、不拦构建。
  这样"公开了什么"始终可见，但不会因为简历里有手机号就卡住发布

## 二、审计会告警什么（告警≠要改）

`npm run pdf-audit` 会打印类似：

```
[pdf] WARN 已豁免文档命中敏感内容（不拦构建，但请确认这些都是可公开信息）：
  public/resume/resume.pdf
    第 1 页  命中禁出词「QAClient」
    第 1 页  命中手机号「13800138000」
```

逐条判断标准：

| 告警类型 | 简历里怎么办 |
|---|---|
| 手机号 / 邮箱 | 保留 —— 简历的联系方式本来就是给雇主的 |
| 姓名 / 学历 / 雇主名称 | 保留 |
| **内部产品名**（如内部 QA 工具链、内部平台名） | 建议**改**：这些是公司业务资产，与站内全站的泛化口径不一致 |
| **内部项目代号**（游戏项目代号） | 建议**改**：未公开的项目名属业务资产 |
| 内网域名 / 端口 / 真实路径 / 工号 | **必改**：这些出现在公开文档里没有任何正当理由 |

> 也就是说：豁免的是"个人身份 + 雇主名称"，不是"业务资产"。
> 若你确认某个内部产品名要保留，就在下一次 `npm run pdf-audit` 的 WARN 里认下来——
> 但要知道它会长期留在公开网络上。

## 三、口径一致性（避免面试官同时看简历与站点撞见两个数字）

| 维度 | 简历 | 站点 | 判定 |
|---|---|---|---|
| Agent 数 | 19 个 | 十余个 | ✅ 兼容 |
| 工具数 | 30+ | 数十个 | ✅ 兼容 |
| 变更记录 | 150+ | 百余条 | ✅ 兼容 |
| 用例量 | 170+ | 上百条 | ✅ 已对齐（站点登记已由"数百条"改为"上百条"） |
| 回归耗时 | 小时级 → 分钟级（**评测**耗时） | 人天级 → 小时级（**回归执行**耗时） | ✅ 已分清对象：两个不同指标，各自写清对象即可，不算冲突 |

结论：口径已无冲突。要点是**同一个词不要指向两个对象**——
站点写"回归执行耗时"，评测那篇写"评测耗时"，简历两处也各自写清对象。

## 四、发布后须知

- PDF 一旦公开，即便之后删除，搜索引擎与第三方聚合站的副本可能仍存在
- 每次更新简历后重跑上面的流程
- 终审结论按 `RELEASE-CHECKLIST.md` 记录，便于追溯线上版本对应哪一版简历
