# RESUME —— 简历上站与脱敏边界

**结论先行：简历本身不脱敏，原样公开。** 简历是主动投递给雇主的对外材料，
姓名、联系方式、雇主名称本来就公开；把公司名泛化成"某公司"反而让人看不出你做过什么。

脱敏只针对**公司业务资产**：内部产品/系统名、内部项目代号、内网地址路径、业务指标精确值。
边界定义见 `GLOSSARY.md` 第〇节。

## 一、上站形态

| 形态 | 位置 | 说明 |
|---|---|---|
| **简历页（主）** | `/resume` | 网页文本：手机可读、可选中、可用浏览器直接打印成 PDF |
| PDF（次） | `public/resume/resume.pdf` | 存在时才显示"下载 PDF 版"入口，作留档与邮件附件 |

- 改简历内容 = 改 `src/pages/resume.astro`
- 换 PDF = 覆盖 `public/resume/resume.pdf` 并 push
- 顶部导航「简历」直达 `/resume`

## 二、闸门对这些文件的态度（豁免 = 仍然告警）

`src/pages/resume.astro` 与 `public/resume/resume.pdf` 都在**豁免清单**里：
命中禁出词只打印 `WARN`，不拦构建。这样"哪些内部标识被公开了"始终可见，
但不会因为简历里有手机号或产品名就卡住上线。当前告警项（本人已确认可公开）：

- 简历页：`QAClient`、`Hunter2`、`Redmine`
- 简历 PDF：同上 + 手机号

逐条判断标准：

| 告警类型 | 简历里的处理 |
|---|---|
| 手机号 / 邮箱 | 保留 —— 简历的联系方式本来就是给雇主的（想降噪就删那一行） |
| 姓名 / 学历 / 雇主名称 | 保留 |
| 内部产品名、内部系统名 | 由你决定；保留意味着它随简历页长期公开 |
| **内部项目代号**（游戏项目代号） | 建议**不要**出现：未公开的项目名属业务资产，且与站内泛化口径冲突 |
| 内网域名 / 端口 / 真实路径 / 工号 | **必改**：出现在公开页面没有任何正当理由 |

## 三、口径一致性（避免面试官同时看简历与站点撞见两个数字）

简历页里的精确值（19 个 Agent、30+ 工具、150+ Spec Change、170+ 用例、80% 覆盖率、
2 pd、30min+、80 人日等）已逐条登记在 `GLOSSARY.md` 第三节，
与站点展示用的弹性区间（十余个 / 数十个 / 百余条 / 上百条）**同基线、不冲突**。

要点是**同一个词不要指向两个对象**：站点写"回归执行耗时"（人天级 → 小时级），
简历写"评测耗时"（小时级 → 分钟级），两者是两个指标，各自写清对象即可。

## 四、发布后须知

- 简历页与 PDF 一旦公开，即便之后修改，搜索引擎与第三方聚合站的副本可能仍存在
- 每次改简历后跑一次 `npm run build`（内部会带 `sanitize` 与 `pdf-audit` 两道告警）
- 终审结论按 `RELEASE-CHECKLIST.md` 记录，便于追溯线上版本对应哪一版简历
