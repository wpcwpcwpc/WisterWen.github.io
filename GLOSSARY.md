# GLOSSARY —— 脱敏词表与泛化映射

> **警示：本文件含内部系统标识清单，仅用于构建前自动扫描。**
> 本仓库 MUST 保持私有；本文件 MUST NOT 复制进 `public/` 或进入任何部署产物。
> 扫描器自身跳过本文件（否则词表命中词表，构建永远失败）。

---

## 〇、脱敏边界（先明确"什么要脱敏"）

**要脱敏的是公司业务资产**，不是个人身份：

| 类别 | 处理 | 说明 |
|---|---|---|
| 内部产品 / 平台 / 系统名 | **泛化** | 见第一节映射表 |
| 内部项目代号（游戏项目、代号） | **泛化** | 未公开的项目名属业务资产 |
| 内网域名 / 端口 / IP / 真实文件路径 / 工号 | **禁止出现** | 见第二节 |
| 业务指标精确值 | **区间化** | 见第三节 |
| 个人身份（姓名、GitHub、公开邮箱） | **可公开** | 对外身份，站点与简历都用 |
| 雇主公开名称、行业域（游戏 QA 等） | **可公开** | 简历里本来就写，站点不必回避——过度泛化会让读者看不出你在做什么 |

> 判断口诀：**这条信息属于"公司没公开的东西"还是"我个人的可公开信息"？**
> 前者脱敏，后者照写。把行业、企业名、个人身份一起抹掉，属于过度脱敏，会削弱可信度。

---

## 一、泛化映射表

写作时遇到左列，一律改成右列对外泛称。

| 内部标识（禁出） | 对外泛称 |
|---|---|
| 游戏项目代号（见禁出词表） | 某游戏项目（或按上下文写"被测游戏"） |
| Hunter2 / hunter2 | 设备农场平台 |
| MBW / MBWorkbench | 服务管理 CLI |
| Redmine | 协作单系统 |
| POPO | 内部即时通讯 |
| 数字工人平台 | 无人值守任务平台 |
| 内部用例平台（见禁出词表） | 内部用例平台 |
| 内部 QA 工具链（QAStudio / QAClient） | 内部 QA 工具链 |
| 客户端调试通道（game_debug_mcp / debug_tool / fast_debug / GMCommon） | 客户端调试通道 |
| qa-auto 自动化主链路 | 自动化主链路 |
| 内部代码托管（maki / git-qa） | 内部代码托管 |
| 配置表 Excel 管线 | 配置表管线 |
| 多家模型与自建推理端点 | 多模型路由（云端多家厂商 + 自建） |
| Agent 编排框架具体名称 | 通用 Agent 编排框架 |
| 内网服务地址与端口（见禁出词表） | 内部服务 |

> 雇主名称（如"网易"）与行业域（如"游戏 QA"）**可直接书写**，不属业务资产；
> 但站内是否点名雇主由作者决定 —— 不写也不等于必须用"某公司"来替代。

---

## 二、禁出词表

扫描器读取本节。**精确词**做子串匹配（大小写不敏感）；**正则模式**做匹配。
新增内部标识时先登记到本节，再开始写作 —— 否则该标识在构建期无人拦截。

### 2.1 精确词

```
Hunter2
hunter2
MBWorkbench
MBW
Redmine
redmine
POPO
popo
H73
h73
maki
git-qa
数字工人
QAStudio
qastudio
QAClient
qaclient
game_debug_mcp
debug_tool
fast_debug
GMCommon
qa_debug
qa-auto
```

> 已移出：`网易 / NetEase / netease` —— 雇主公开名称不属业务资产（个人身份与雇主名可公开）。
> 内网域名仍由 2.2 的正则拦截（`*.corp.netease.com` 之类）。

### 2.2 正则模式

| 模式 | 说明 |
|---|---|
| `[\w.-]+\.(netease\|corp\|internal)\.com` | 内网域名与内网邮箱后缀 |
| `localhost:\d{2,5}` | 本机服务地址 |
| `127\.0\.0\.1:\d{2,5}` | 本机服务地址 |
| `\d{1,3}(\.\d{1,3}){3}:\d{2,5}` | 内网 IP + 端口 |
| `\b\d{1,3}(\.\d{1,3}){3}\b` | 裸内网 IP |
| `[A-Za-z]:\\[\w\\.-]+` | Windows 真实文件路径 |
| `/(home\|data\|srv)/[\w/.-]+` | Linux 真实文件路径 |
| `svn://[\w.:/-]+` | 内部版本库地址 |
| `git@[\w.-]+:[\w/.-]+` | 内部代码托管 SSH 地址 |
| `(?<![\w@.])\d{6,9}(?![\w@.])` | 工号 / 内部编号（前后加环视，避免误伤邮箱与域名里的数字） |

---

## 三、指标区间化登记（口径单一来源）

**本节是全站量化事实的唯一定义处。** 对外出现的每个指标 MUST 在此登记其区间或倍数表述；
内容中 MUST NOT 出现精确内部数字。项目页 frontmatter 的 `figures` 只存放"页面上展示什么"，
其取值由 `npm run figures` 强制与本节逐字一致 —— 改一处不同步另一处，构建即失败。

「来源说明」写统计口径（数据从哪里数出来的），MUST NOT 写内部文件路径或函数名。

| 指标项 | 对外表述 | 来源说明 |
|---|---|---|
| 专职 Agent 数量 | 十余个 | 按内置 Agent 注册表中实际注册生效的条目计数，不含按需热加载的自定义 Agent |
| 内置工具数量 | 数十个 | 按工具注册表内内置工具条目计数，不含按技能与外部能力动态注册的工具 |
| 管控 Hook 数量 | 近二十道 | 按挂载到运行链路上的 Hook 模块计数，不含其依赖的工具函数与辅助模块 |
| 流水线步骤数 | 四步 | 按自动化主链路上实际编排的步骤计数：规划 → 环境就绪 → 执行 → 收尾 |
| 能力规格数量 | 四十余项 | 按规格库中已建档的能力规格计数 |
| 工程变更记录量 | 百余条 | 按变更记录库中已归档的变更条目计数，随项目演进持续增加 |
| 回归执行耗时 | 从人天级降到小时级 | 对象是**一轮回归的执行耗时**（人工串联 → 平台执行）；与"评测耗时（小时级→分钟级）"是两个不同指标，不可混用 |
| 累计沉淀用例量 | 上百条 | 已沉淀场景与用例总量；"上百条"与简历里的 170+ 兼容 |
| 长时任务连续运行时长 | 数小时级 | 无人值守长链路任务的连续运行时长区间 |

> 未登记的指标不得出现在内容中；新增指标先登记后写作。
> 登记值之间 MUST NOT 互相矛盾（如"十余个 Agent"与"40+ Agent"不可并存）。

---

## 四、待定项（需人工确认口径）

- `qa-agent` / `QAClient` / `QAStudio` 是否视为内部标识？当前判断：`qa-agent` 属通用描述词（"QA Agent"），允许出现在项目 slug 与正文；`QAClient` / `QAStudio` 按内部产品名处理，禁出。若认为 `qa-agent` 也需规避，请在此登记后同步修改项目 slug（注意：slug 一经发布不得变更）。
- 游戏项目代号一律泛化（业务资产）；行业域"游戏 QA"可直接书写（已确认）。
- 雇主名称（网易）不属业务资产，可公开；站内是否点名雇主由作者决定（当前站内未点名）。
- 效率类指标的区间表述与简历里的精确值需保持不冲突（见 `RESUME.md` 第三节）。
