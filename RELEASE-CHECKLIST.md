# RELEASE CHECKLIST —— 发布前自检与人工终审

每次发布前逐条执行。终审结论记入文末「终审记录」，使发布过程可追溯。

## 一、机器闸门（自动）

- [ ] `npm run build` 全程通过（脱敏扫描 → 口径校验 → 专栏校验 → PDF 审计 → 图表生成 → deck → OG → 站点构建）
- [ ] 脱敏扫描输出 `PASS 未命中禁出词`
- [ ] 词表两处同步：本地 `GLOSSARY.local.md` 与仓库 secret `GLOSSARY_WORDS` 内容一致
      （不一致 → 本地假通过 / CI 漏拦，词表本身不在仓库里，没有别的对账机制）
- [ ] 口径校验输出 `PASS 页面取值与登记一致`（页面数字与 `GLOSSARY.md` 第三节逐字一致）
- [ ] 专栏校验输出 `PASS 章节与专栏登记一致`（章节专栏/分区/序号与 `src/series.ts` 一致）
- [ ] PDF 审计输出 `PASS 待发布 PDF 未命中敏感内容`（含简历：无手机号等 PII）
- [ ] 链接检查输出 `PASS 无死链`
- [ ] 构建产物中不含 `GLOSSARY.md`、`GLOSSARY.local.md`、`SHOTS.md`、`RESUME.md`、`SERIES.md`、`.env`
- [ ] 仓库侧（含 `openspec/` 与根级 `*.md`）没有任何内部标识：词表只在私有侧（脱敏扫描已覆盖这几处）

## 二、口径一致性

- [ ] 内容中出现的每个量化事实都能在 `GLOSSARY.md` 第三节「指标区间化登记」找到来源
- [ ] 项目页展示的数字全部由 frontmatter `figures` 取值，正文里没有另写一套数字
- [ ] 不存在两处对同一事实给出不同数字
- [ ] 每个数字都能回答"怎么来的"（登记表的「来源说明」列）

## 三、脱敏（人工语义判断）

- [ ] 按"假设前雇主与现雇主都能看到"的标准通读全文
- [ ] 所有内部系统名已按泛化映射表替换（对照私有词表 `GLOSSARY.local.md` 第一节）
- [ ] 指标全部为区间或倍数表述，无精确内部数字
- [ ] 图表无内部代号、无内网地址端口、无真实路径（逐图对照私有词表 `GLOSSARY.local.md` 第一节）
- [ ] 无真实业务代码（含架构示意代码块，≤15 行且为伪代码）
- [ ] 若放了简历 PDF：已按 `RESUME.md` 出对外脱敏版（无手机号 / 出生年月 / 性别，公司名与内部产品名已泛化）
- [ ] 简历与站点的口径无矛盾（Agent 数、回归效率、变更记录、用例量）
- [ ] `node scripts/check-pdf.mjs --dump` 已人工通读一遍待发布 PDF
- [ ] 联系渠道仅邮箱 / 代码托管主页，无手机号、无内部通讯账号
- [ ] 内容为重新撰写，无内部文档原文搬运

## 四、占位与截图

- [ ] `SHOTS.md` 状态与页面上实际占位一致
- [ ] 已覆盖的截图逐条确认过「脱敏注意」项
- [ ] 未覆盖的占位以统一视觉呈现（无破图、无空白块）

## 五、站点可用性

- [ ] 示例内容已删除（`projects/sample-project.md`、`posts/sample-post.md` 均已移除，产物中无 `/projects/sample-*`、`/posts/sample-*`）
- [ ] `src/site.config.ts` 中的 TODO 占位已全部替换（站点名 / 定位 / 邮箱 / 代码托管）
- [ ] 所有 `/projects/*`、`/posts/*`、`/claudecode/*` 链接可访问，无 404（`npm run check` 通过）
- [ ] 手机真机访问：正文可读、导航可用、无横向滚动
- [ ] 分享预览：链接粘贴到聊天工具能出标题 / 摘要 / 缩略图
- [ ] PDF 入口可用，或显示"生成中"而非死链
- [ ] 简历入口可用（`/resume`：内联渲染简历 PDF）
- [ ] deck 入口可用，或显示为待补齐状态而非死链
- [ ] 项目页首屏（不滚屏）能读到定位、关键数字与架构总览图
- [ ] 文章页：首页只出专栏卡片 + 最近单篇，`/posts` 分区与分页可达

## 六、专栏与外部导入（见 `SERIES.md`）

- [ ] 导入只走白名单号段：仓库内无 25–28 号段内容（`git log --stat` 与 `src/content/posts/` 双向确认）
- [ ] 导入报告无未处置项：「代码围栏不配对」「未解析的章节引用」都已核对
- [ ] 抽查 3 篇章节：内网标识已改写、章节互链可达、元数据（序号/分区/时长/星级）正确
- [ ] 导入产物 frontmatter 的 `source` 只有文件名，无源目录绝对路径
- [ ] 拆解类材料的**可公开性已由作者确认**（反编译/源码阅读所得的公开分析属合规灰区，发布前须拍板）
- [ ] 源文档与站点内容口径不冲突（同一条事实两边措辞一致）

## 七、终审记录

| 日期 | 版本（提交哈希） | 结论 | 备注 |
|---|---|---|---|
| （待记录） | | | |
